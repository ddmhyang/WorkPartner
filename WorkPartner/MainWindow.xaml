<Window x:Class="WorkPartner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkPartner"
        mc:Ignorable="d"
        Title="WorkPartner" Height="720" Width="1280" Icon="/WorkPartner.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#34495E">
            <StackPanel Margin="10">
                <TextBlock Text="WorkPartner" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,30"/>
                <Button Content="대시보드" Click="DashboardButton_Click" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="아바타 꾸미기" Click="AvatarCustomizationButton_Click" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="통계" Click="AnalysisButton_Click" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="설정" Click="SettingsButton_Click" Style="{StaticResource NavButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Frame Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A6572"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>
