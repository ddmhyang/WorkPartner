<UserControl x:Class="WorkPartner.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkPartner"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="HandlePreviewMouseWheel">
            <StackPanel Margin="15">
                <GroupBox Header="프로필 설정" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="닉네임:" Margin="0,0,0,5"/>
                        <TextBox x:Name="NicknameTextBox" TextChanged="NicknameTextBox_TextChanged"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="과목별 색상 설정" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="과목을 더블 클릭하여 색상을 변경하세요. (#AARRGGBB 형식)" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray" FontSize="11"/>
                        <ListBox x:Name="TaskColorsListBox" MaxHeight="200" MouseDoubleClick="TaskColorsListBox_MouseDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="16" Height="16" Background="{Binding ColorBrush}" BorderBrush="Gray" BorderThickness="1" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="타이머 및 추적 설정" Margin="0,0,0,10">
                    <StackPanel>
                        <CheckBox x:Name="IdleDetectionCheckBox" Content="자리 비움 감지 활성화" Margin="0,0,0,5" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,10">
                            <TextBlock Text="다음 시간(초) 동안 활동이 없으면 자리 비움으로 간주:" VerticalAlignment="Center"/>
                            <TextBox x:Name="IdleTimeoutTextBox" Width="50" Margin="5,0,0,0" TextChanged="Setting_Changed_IdleTimeout"/>
                        </StackPanel>
                        <CheckBox x:Name="MiniTimerCheckBox" Content="미니 타이머 활성화" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="집중 모드 설정" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="경고 메시지:" Margin="0,0,0,5"/>
                        <TextBox x:Name="NagMessageTextBox" TextChanged="NagMessageTextBox_TextChanged" Margin="0,0,0,10"/>
                        <TextBlock Text="경고 간격 (초):" Margin="0,0,0,5"/>
                        <TextBox x:Name="NagIntervalTextBox" TextChanged="NagIntervalTextBox_TextChanged"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="AI 태그 규칙" Margin="0,0,0,10">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="KeywordInput" Grid.Column="0" Margin="0,0,5,0" Text="키워드"/>
                            <TextBox x:Name="TagInput" Grid.Column="1" Margin="0,0,5,0" Text="#태그"/>
                            <Button Content="규칙 추가" Grid.Column="2" Click="AddTagRuleButton_Click"/>
                        </Grid>
                        <ListView x:Name="TagRulesListView" ItemsSource="{Binding TagRules}" MaxHeight="150" Margin="0,10,0,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="키워드" DisplayMemberBinding="{Binding Key}"/>
                                    <GridViewColumn Header="자동 태그" DisplayMemberBinding="{Binding Value}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        <Button Content="선택한 규칙 삭제" Click="DeleteTagRuleButton_Click" Margin="0,5,0,0" HorizontalAlignment="Right"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="데이터 관리" Margin="0,0,0,10">
                    <Button Content="모든 데이터 초기화" Background="IndianRed" Foreground="White" Click="ResetDataButton_Click"/>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Right Column -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="HandlePreviewMouseWheel">
            <StackPanel Margin="15">
                <local:ProcessRegistrationControl Title="학습/작업 프로그램 및 사이트 등록" ProcessListType="Work" DataContext="{Binding}"/>
                <local:ProcessRegistrationControl Title="수동/패시브 프로그램 등록" ProcessListType="Passive" DataContext="{Binding}" Margin="0,10,0,0"/>
                <local:ProcessRegistrationControl Title="방해 프로그램 및 사이트 등록" ProcessListType="Distraction" DataContext="{Binding}" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

